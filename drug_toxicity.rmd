---
title: "Analyse de données de la toxicité du paracetamol sur le foie"
author: "Rabah ACHOUR, Mohand Arezki BRANECI"
date: '' 
output:
  pdf_document: default
---

# A) Motivation
**On souhaite étudier l'influence du paracetamol sur la toxicité hépatique à partir d'un jeu de données transcriptomiques sur des rats.**
**On dispose de données d'expression génique (3116 gènes) mesurées sur 64 rats exposés à différentes doses de paracetamol.**
**L'objectif est d'identifier les gènes dont l'expression est significativement affectéee par la dose de paracetamol.**

&nbsp;

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
rm(list=ls())
library(MultiVarSel)
library(ggplot2)
```

# B) Exploration des données 

### 1)  Examination brute des données
```{r}
load("liver_data.Rdata")
ls()
```
**On a un objet liver.toxicity. Regardons la structure globale de l'objet**

&nbsp;

```{r}
summary(liver.toxicity)
```
**On a 4 éléments dans l'objet liver.toxicity. Regardons chacun d'eux.**

&nbsp;

**`treatment`**
```{r}
table1 = liver.toxicity$treatment
str(table1)
table1[1:3, ]
```
**La colonne `Treatment.Group` contient la même information que `Dose.Group`, enlevons-la.**
```{r}
table1$Treatment.Group <- NULL
table1$Time.Group <- NULL
table1[1:3, ]
```

&nbsp;

**Examinons la répartition du facteur `Dose.Group`.**
```{r}
table(table1$Dose.Group)
```
La distribution des échantillons est parfaitement équilibrée.  
Chaque niveau de *Dose.Group* (50, 150, 1500, 2000) contient 16 échantillons.  

&nbsp;

**`gene`**
```{r}
table2 = liver.toxicity$gene
dim(table2)
table2[1:3, 1:5]
```

&nbsp;

**`clinic`**
```{r}
table3 = liver.toxicity$clinic
str(table3)
table3[1:3, ]
```

&nbsp;

**`gene.ID`**
```{r}
table4 = liver.toxicity$gene.ID
str(table4)
table4[1:3, ]
```

&nbsp;

**On a donc 4 dataframes dans l'objet ``liver.toxicity``**

- **gene** : données d’expression génique  
  $\mathbf{G} \in \mathbb{R}^{64 \times 3116}$  
  64 échantillons $\times$ 3116 gènes (valeurs numériques).

- **clinic** : variables cliniques  
  $\mathbf{C} \in \mathbb{R}^{64 \times 10}$  
  64 échantillons $\times$ 10 mesures cliniques (numériques et entières).
- **treatment** : groupes de traitement  
  $\mathbf{T} \in \mathbb{R}^{64 \times 2}$  
  64 échantillons $\times$ 3 descripteurs (numéro d’animal, dose [numérique], temps [entier]).

- **gene.ID** : identifiants des gènes  
  $\mathbf{I} \in \mathbb{R}^{3116 \times 2}$  
  3116 gènes $\times$ 2 champs d’annotation (geneBank, gene.title en facteurs).  

&nbsp;


### 2) Valeurs manquantes 

&nbsp;

```{r}
which(is.na(table1))
which(is.na(table2))
which(is.na(table3))
```

**On n'a pas de valeurs manquantes. Concentrons nous sur les données `gene` et `treatment`**.

&nbsp;

### 3) Visualisation des données de gènes

&nbsp;

Les données d’expression génique sont de haute dimension. Pour une visualisation de la variabilité, on peut utiliser une analyse en composantes principales (ACP).

&nbsp;

```{r}
pca_samples <- prcomp(table2, scale. = TRUE)
```

&nbsp;

**Regardons la variance expliquée par chaque composante principale.**
```{r}
var_explained <- pca_samples$sdev^2 / sum(pca_samples$sdev^2)
round(var_explained[1:10], 3)
```
**Les deux premières composantes principales expliquent une part significative de la variance totale : PC1 capture 28.1 % et PC2 14.9 %, soit 43 % à elles deux.** 
**Cela indique que l’expression génique présente une structure non aléatoire.**

&nbsp;

**Projection des échantillons avec coloration et formes par facteurs Dose et Périodicité**
```{r}
df_pca <- data.frame(PC1 = pca_samples$x[,1],
                     PC2 = pca_samples$x[,2],
                     Dose = factor(table1$Dose.Group))
ggplot(df_pca, aes(x = PC1, y = PC2, color = Dose)) +
  geom_point(size = 3) +
  labs(title = "ACP des échantillons selon dose",
       x = paste0("PC1 (", round(var_explained[1]*100,1), "%)"),
       y = paste0("PC2 (", round(var_explained[2]*100,1), "%)")) +
  theme_minimal()
```


&nbsp;

**On observe une séparation partielle des échantillons selon la dose, suggérant que ce facteur influence l’expression génique.**
**Cette ACP justifie une analyse plus approfondie pour quantifier cet effet.**

&nbsp;

### 4) Réduction de la dimensionnalité

&nbsp;

**Pour alléger les calculs de la selection de variables, on peut réduire la dimensionnalité en sélectionnant les gènes les plus variables.**
**Le choix de 500 gènes vient d'un calcul empirique basé sur la RAM disponible (15 Go).**

```{r}
gene_importance <- rowSums(pca_samples$rotation[, 1:10]^2)
k <- 500
selected_genes <- order(gene_importance, decreasing = TRUE)[1:k]
table2_reduced <- table2[, selected_genes]
dim(table2_reduced)
```
&nbsp;

# C) Modélisation linéaire multivariée à un facteur
### 1) Intuition de la modélisation

&nbsp;

Le paracétamol est connu pour provoquer une toxicité hépatique à forte dose.  
Dans notre jeu de données, nous avons un facteur expérimental :

- **Dose.Group** : la quantité de paracétamol administrée aux rats.  

Notre hypothèse est que :

- L’expression de certains gènes hépatiques **varie en fonction de la dose** administrée.  

Cette intuition justifie l’utilisation, d'un **modèle linéaire multivarié** pour tester :

- l’effet principal de la dose.  

### 2) Construction de la matrice de design $X$

&nbsp;

Pour ajuster un modèle linéaire multivarié, on construit la matrice de design $X \in \mathbb{R}^{64 \times p}$ qui encode le facteur expérimental *Dose.Group*.

Le modèle multivarié s’écrit :
$$
Y = X\beta + \varepsilon
$$

où :

- $Y \in \mathbb{R}^{64 \times 1500}$ contient les expressions des 1500 gènes,
- $X$ contient les colonnes indicatrices (contrastes) associées à la dose,
- $\beta \in \mathbb{R}^{p \times 1500}$ est la matrice des coefficients estimés pour tous les gènes simultanément,
- $\varepsilon$ est la matrice des résidus.

Dans notre cas, la matrice $X$ contient :

- une colonne d’intercept,
- trois colonnes pour les contrastes de dose (4 niveaux $\rightarrow$ 3 degrés de liberté),

Ainsi,
$$
p = 1 + 3 = 4.
$$

donc 
$$
X \in \mathbb{R}^{64 \times 4}.
$$


&nbsp;

```{r}

table_XY <- data.frame(table1[-1], table2_reduced)
table_XY$Dose.Group <- factor(table_XY$Dose.Group)
table_XY[1:3, 1:10]
dim(table_XY)
```

&nbsp;

**Matrice Y et X**

**Modélisation par un modèle linéaire multivarié**
```{r}
Y <- as.matrix(table2_reduced)
Y[1:3, 1:5]
X <- model.matrix(lm(Y ~ Dose.Group + 0, data = table_XY))
X
dim(X)
colnames(X)

Y <- scale(Y)
Y[1:3, 1:5]

```
**On a bien les coefficients pour chaque gène (1500) et chaque paramètre du modèle**

&nbsp;


### 3) Analyse des résidus du modèle

&nbsp;

**Pour vérifier la validité du modèle multivarié et faire une selection stable de gènes , examinons les résidus et leurs propriétés**

- 1) **Hypothèse d'absence d'autocorrélation** : Les résidus doivent être indépendants entre eux, équivalents à un bruit blanc..


&nbsp;

```{r}
# residus = fit$residuals
residus = lm(as.matrix(Y) ~ X - 1)$residuals
dim(residus)
pvalue = whitening_test(residus)
print(pvalue)
dim(cov(residus))

```

&nbsp;

**La p-valeur du test de blanchiment est très faible. Cela indique une extrême corrélation des résidus et que l'hypothèse d'absence d'autocorrélation est rejetée.**

&nbsp;

# D) Modélisation de la dépendance des résidus et sélection de variables

### 4) Modélisation de la dépendance des résidus par estimation de la matrice de covariance $\Sigma$
**Il existe plusieurs façons de modéliser la dépendance entre les résidus. Ces approches se distinguent par la manière dont elles estiment la matrice de covariance des erreurs :**

- **Modèle AR(1)** : cas particulier du modèle autorégressif où chaque résidu dépend uniquement du précédent (structure très simple et entièrement paramétrique).
- **Modèle ARMA** : suppose que les résidus suivent un processus autorégressif à moyenne mobile (structure paramétrique flexible mais imposée).
- **Modèle non paramétrique** : aucune contrainte paramétrique n'est imposée ; la matrice de covariance est estimée directement et peut capturer des dépendances plus complexes

**On va comparer ces trois approches pour choisir la plus adaptée aux données.**
```{r}
res_choice <- whitening_choice(residus, typeDeps = c("AR1", "nonparam", "ARMA"), pAR = 1, qMA = 1)
res_choice
print(res_choice$Pvalue)
```

&nbsp;

**On observe que la p-valeur la plus élevée est obtenue avec le modèle de Toeplitz non paramétrique, suggérant qu'il capture mieux la structure de dépendance des résidus.**
**On va donc utiliser cette méthode pour le blanchiment des résidus.**

&nbsp;

**Vérifions la différence des deux p-valeurs entre le modèle AR(1) et le modèle Toeplitz non paramétrique.**
```{r}
# Whitening AR(1)
sqrt_inv_sigma_AR1 <- whitening(residus, "AR1", pAR = 1, qMA = 0)

# Whitening Toeplitz Non-paramétrique
sqrt_inv_sigma_nonparam <- whitening(residus, "nonparam", pAR = 1, qMA = 0)

# Résidus blanchis avec Toeplitz non-param
residus_nonparam <- residus %*% as.matrix(sqrt_inv_sigma_nonparam)

# Résidus blanchis AR(1)
residus_AR1 <- residus %*% as.matrix(sqrt_inv_sigma_AR1)

pvalue_non_param = whitening_test(residus_nonparam)
print(pvalue_non_param)

pvalue_AR1 = whitening_test(residus_AR1)
print(pvalue_AR1)
```

**On voit que la p-valeur du modèle $AR(1)$ est trés faible, ce qui est trés loin d'une p-valeur acceptable.**

&nbsp;

### 5) Sélection des gènes influencés par les facteurs expérimentaux par la Stability Selection

**Maintenant que nous avons stabilisé les résidus via le blanchiment, nous pouvons procéder à la sélection des gènes influencés par le facteur dose.**
**Nous allons utiliser la Stability Selection, une méthode robuste pour identifier les variables pertinentes dans un contexte de haute dimension.**
**L'idée est de répéter plusieurs fois le processus de sélection sur des sous-échantillons des données et de calculer la fréquence de sélection de chaque gène apparu dans les couples variable-réponse.**

**Cette méthode peut-être trés lourde car elle nécessite le calcul de la matrice de Kronecker. Ici...**

&nbsp;

```{r}
# require(doMC) 
# registerDoMC(cores = 3)
# Freqs = variable_selection(Y, X, sqrt_inv_sigma_nonparam, nb_repli = 200, parallel = TRUE, nb.cores = 3)
# save(Freqs, file = 'Freqs_liver_toxicity_TOEPLITZ_nbreplis_200.Rdata')
load('Freqs_liver_toxicity_TOEPLITZ_nbreplis_200.Rdata')
```

```{r}
dim(X)
```

&nbsp;

**Renommons les colonnes de Freqs pour plus de clarté**
```{r}
colnames(Freqs) <- c('Names_of_Y', 'Names_of_X', 'frequency')
```

&nbsp;

**Visualisons la fréquence des couples variables-réponse**
```{r, fig.width=10, fig.height=6}
plot(sort(Freqs$frequency, decreasing = T), type = 's', 
     main = "Fréquence de sélection des couples variables-réponse",
     xlab = "Couples Variables-Réponse (triées par fréquence)", ylab = "Fréquence de sélection")
sort(Freqs$frequency,decreasing = T)[1:50]

```
**

**Pour sélectionner les gènes les plus influencés, vu le contexte de santé médicale, fixons un seuil de fréquence de sélection à 0.93, ce qui signifie que l'on retient les gènes sélectionnés dans au moins 93 % des répétitions.**
```{r}
# Fixer un seuil de fréquence de sélection
seuil <- 0.93
indices <- which(Freqs$frequency >= seuil)
length(indices)

# Gènes sélectionnés
selected_genes <- unique(Freqs[indices, ]$Names_of_Y)
length(selected_genes)

# Vérifier combien de couples liés à la dose sont retenus
sum(grepl("Dose", Freqs$Names_of_X) & (1:nrow(Freqs) %in% indices))

```

&nbsp;

**193 gènes sont séléctionnés avec ce seuil.**

&nbsp;

# E) Visualisation et interprétation des résultats

**Pour avoir des visuels lisibles, on va regarder les 15 gènes les plus influencés**
**Nous avons besoin de réestimer les coefficients pour ces gènes car le LASSO introduit un biais.**

**Reestimation des coefficients pour les variables sélectionnés**
```{r}
# # Étape 1 : construction des vecteurs Yvec et Xvec
# Yvec <- as.numeric(Y %*% sqrt_inv_sigma_nonparam)

# Xvec <- kronecker(t(sqrt_inv_sigma_nonparam), X)

# # Étape 2 : sélection des colonnes correspondant aux variables retenues
# Xvec_sel <- Xvec[, indices]

# # Étape 3 : estimation des coefficients (solution des moindres carrés)
# B_sel_hat <- solve(t(Xvec_sel) %*% Xvec_sel, t(Xvec_sel) %*% Yvec)

# # Étape 4 : stockage dans Freqs
# p <- ncol(X)
# q <- ncol(Y)
# Freqs$estim <- rep(0, p * q)
# Freqs$estim[indices] <- as.vector(B_sel_hat)
# # Save Freqs with estim
# save(Freqs, file = 'Freqs_liver_toxicity_TOEPLITZ_nbreplis_200_with_estim.Rdata')
load('Freqs_liver_toxicity_TOEPLITZ_nbreplis_200_with_estim.Rdata')
```

&nbsp;

#### Sélection des 15 gènes les plus influencés par les valeurs réestimés.

&nbsp;

```{r}
library(dplyr)

# --- Sélection des 15 plus influents POUR DOSE
Freqs_dose <- Freqs %>%
  filter(grepl("Dose", Names_of_X), frequency >= seuil)

top15_dose <- Freqs_dose %>%
  arrange(desc(abs(estim))) %>%
  head(15)

top15_dose$Effet <- top15_dose$Names_of_X
top15_dose
```

&nbsp;

#### Heatmap des effets des facteurs sur 10 gènes parmi les gènes sélectionnés

&nbsp;

```{r}
library(ggplot2)
library(dplyr)

# Récupérer les noms lisibles des gènes depuis table4
top15_dose <- top15_dose %>%
  left_join(table4, by = c("Names_of_Y" = "geneBank")) %>%
  mutate(GeneName = ifelse(!is.na(gene.title), gene.title, Names_of_Y))

# Heatmap : gènes (axe X) vs effets Dose (axe Y)
gr <- ggplot(top15_dose, aes(
    x = GeneName,
    y = Effet,
    color = estim
)) +
    geom_point(size = 5) +
    scale_color_gradient2(low = "blue", mid = "grey", high = "red") +
    theme_bw() +
    theme(
        axis.text.x = element_text(angle=90, hjust=1),
        text = element_text(size=14)
    ) +
    ggtitle("Top 15 gènes les plus influencés par la Dose")

print(gr)

# Sauvegarde
ggsave("heatmap_top15_dose.png", plot = gr, width = 12, height = 8)
```

**On observe que certains gènes sont fortement influencés par le facteur dose**

&nbsp;

#### Boxplots de 10 gènes parmi les gènes sélectionnés

&nbsp;

```{r, fig.width=18, fig.height=20}

library(reshape2)
library(ggplot2)
library(dplyr)

# Gènes à tracer
genes_to_plot <- top15_dose$Names_of_Y

# Construction du tableau réduit
table_red <- as.data.frame(Y[, genes_to_plot, drop = FALSE])

# Ajout de la colonne Dose
table_red$Dose <- factor(table_XY$Dose.Group)

# Passage au format long
table_long <- melt(table_red, id.vars = "Dose")

# Forcer les noms de gènes comme facteur pour affichage correct
table_long$GeneLabel <- factor(table_long$variable, levels = genes_to_plot)

# Création du graphique
g <- ggplot(table_long, aes(x = Dose, y = value, fill = Dose)) +
    geom_boxplot() +
    facet_wrap(~GeneLabel, scales = "free_y", ncol = 3) +
    theme_bw() +
    theme(
        axis.text.x = element_text(size=12, angle=45, hjust=1),
        axis.text.y = element_text(size=12),
        strip.text = element_text(size=14, face="bold"),
        strip.background = element_rect(fill = "lightgrey", color = "black"),
        legend.position = "right",
        legend.title = element_text(size=13),
        legend.text = element_text(size=12)
    ) +
    ylab("Expression normalisée") +
    xlab("Dose") +
    ggtitle("Top 15 gènes les plus influencés par la Dose")

print(g)

# Sauvegarde
ggsave("boxplot_top15_dose.png", plot = g, width = 16, height = 12)

```

&nbsp;

**On voit que les gènes sélectionnés montrent des variations d'expression significatives en fonction de la dose.**

&nbsp;

#### Dendrogramme de 10 gènes parmi les gènes sélectionnés

&nbsp;

```{r }
# Sélection des gènes
liste_genes_selected <- unique(top15_dose$Names_of_Y)
length(liste_genes_selected)

# Extraction des résidus pour ces gènes
residus_red <- residus[, colnames(residus) %in% liste_genes_selected]

# Construction du dendrogramme
hc <- hclust(dist(1 - abs(cor(residus_red))))
plot(hc, main = "Dendrogramme des gènes influencés par la Dose")
rect.hclust(hc, k = 3)

# Sauvegarde du graphique
dev.copy(png, "Dendrogram_top15_genes_dose.png", width = 800, height = 600)
dev.off()
```

&nbsp;

**On observe des clusters de gènes avec des profils d'expression similaires parmi les gènes sélectionnés, ce qui peut indiquer des régulations transcriptionnelles communes en réponse à la dose.**
**Cela suggère que ces gènes pourraient être impliqués dans des voies biologiques similaires affectées par le paracétamol.**

# F) Difficultés rencontrées

&nbsp;

**La matrice de Design X a posé beaucoup de confusion à cause de l'oubli d'utilisation de `factor` pour le facteur Dose.Group, ce qui a conduit à des dimensions incorrectes.**
&nbsp;
**Modéliser X avec 2 facteurs (Dose et Temps) dès le début a provoqué cette confusion avec des mauvaises dimensions, on a décidé pour le moment de ne modéliser que l'effet principal de la Dose.**

&nbsp;

**La deuxieme grande difficulté a été la lourdeur computationnelle de la Stability Selection, en particulier le calcul de la matrice de Kronecker, gènes**
**Pour alléger les calculs, nous avons réduit la dimensionnalité en sélectionnant les 500 gènes les plus variables.**

&nbsp;

**Le choix du modèle de dépendance des résidus a également été crucial pour obtenir des résidus blanchis satisfaisants.**
**Dans un premier temps, à cause d'une autre approche de modélisation, nous avions eu des p-valeurs de blanchiments trés faibles pour les 3 familles de modèles, ce qui nous a permit d'explorer la modélisation par les matrices de covariances par blocs.**


&nbsp;

**NB: Pour la fonction `whitening_choice` de `MultiVarSel`, les p-valeurs affichées peuvent s'afficher ZERO alors qu'elles sont supérieur au seuil enregistreable de R, c'est à dire inférieur à environ $10^{-301}$**

&nbsp;

# G) Conclusion

**Ce projet fut trés formateur et a permis d'appliquer des techniques avancées de modélisation statistique multivariée à un problème réel de toxicité médicamenteuse.**

&nbsp;

**La mise en situation dans un environnement complexe par la contrainte des ressources disponibles a été un défi intéressant à relever.**


